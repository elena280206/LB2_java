public class Main {
    /**
     * Точка входа в программу.
     * Здесь происходит ввод Markdown-ссылки и вывод результата в формате HTML.
     */
    public static void main(String[] args) {
        UserInterface ui = new UserInterface();
        ui.run();
    }
}
